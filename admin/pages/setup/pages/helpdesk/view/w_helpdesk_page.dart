import 'package:flutter/material.dart';

class WebHelpDeskPage extends StatelessWidget {
  const WebHelpDeskPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Help desk setup page'),
      ),
    );
  }
}
